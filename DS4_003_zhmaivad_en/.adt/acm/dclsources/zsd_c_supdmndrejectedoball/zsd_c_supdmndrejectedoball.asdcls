@EndUserText.label: 'Restricted'
@MappingRole: true

define role ZSD_C_SUPDMNDREJECTEDOBALL {
    grant
        select
            on
                ZSD_C_SUPDMNDREJECTEDOBALL
                    where
                        inheriting conditions from entity C_SupDmndAllDocTotDmndQry;
                        
}