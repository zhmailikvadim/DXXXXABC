@EndUserText.label: 'Unrestricted'
@MappingRole: true
define role ZSD_I_ACTUAL_PICKED_STOCK {
    grant
        select
            on
                ZSD_I_ACTUAL_PICKED_STOCK where inheriting conditions from entity ZSD_I_BW_SNAPSHOT_CALC_DIFFER; 
                
}