@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZSD_I_BILLINGDOCUMENSUM {
    grant
        select
            on
                ZSD_I_BILLINGDOCUMENSUM
        where ( ) =
        aspect pfcg_auth (  V_VBRK_FKA,
                            actvt = '03' );
                        
}