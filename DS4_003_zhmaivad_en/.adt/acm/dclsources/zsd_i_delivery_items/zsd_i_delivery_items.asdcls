@EndUserText.label: 'Unrestricted'
@MappingRole: true
define role ZSD_I_DELIVERY_ITEMS {
    grant
        select
            on
                ZSD_I_DELIVERY_ITEMS where inheriting conditions from entity zsd_i_stock_difference_calc_p1; 
                
}