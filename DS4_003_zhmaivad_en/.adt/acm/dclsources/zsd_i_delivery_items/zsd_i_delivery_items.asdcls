@EndUserText.label: 'Unrestricted'
@MappingRole: true
define role ZSD_I_DELIVERY_ITEMS {
    grant
        select
            on
                ZSD_I_DELIVERY_ITEMS where inheriting conditions from entity ZSD_I_BW_SNAPSHOT_CALC_DIFFER; 
                
}