@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZSD_I_LEAD_TIMES_LAST_DATE {
    grant
        select
            on
                ZSD_I_LEAD_TIMES_LAST_DATE
                    where
                        inheriting conditions from entity zsd_i_outboundschedulinglt;
                        
}