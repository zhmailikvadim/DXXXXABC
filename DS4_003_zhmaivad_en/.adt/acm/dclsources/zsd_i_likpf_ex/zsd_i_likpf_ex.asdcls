@EndUserText.label: 'SD Document: Delivery Header Data'
@MappingRole: true
define role ZSD_I_LIKPF_EX {
    grant
        select
            on
                ZSD_I_LIKPF_EX
                     where (  ) = aspect pfcg_auth ( ZIS_CDS, CDS_NAME = 'ZSD_I_LIKPF_EX' ); 
}