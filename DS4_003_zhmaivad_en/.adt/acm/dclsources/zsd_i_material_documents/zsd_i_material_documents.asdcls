@EndUserText.label: 'Unrestricted'
@MappingRole: true
define role ZSD_I_MATERIAL_DOCUMENTS {
    grant
        select
            on
                ZSD_I_MATERIAL_DOCUMENTS where inheriting conditions from entity zsd_i_stock_corr;
                
}