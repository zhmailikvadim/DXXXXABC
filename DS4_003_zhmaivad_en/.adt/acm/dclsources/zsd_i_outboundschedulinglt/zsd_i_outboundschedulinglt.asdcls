@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZSD_I_OUTBOUNDSCHEDULINGLT {
    grant
        select
            on
                ZSD_I_OUTBOUNDSCHEDULINGLT 
                          where ( ) = aspect pfcg_auth ( I_TCODE, TCD = 'IQ09' );
                        
}