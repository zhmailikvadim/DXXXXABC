@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZSD_I_PRODUCT_THEME_TEXT {
    grant
        select
            on
                ZSD_I_PRODUCT_THEME_TEXT
                    where
                        inheriting conditions from entity I_ProductTheme;
                        
}