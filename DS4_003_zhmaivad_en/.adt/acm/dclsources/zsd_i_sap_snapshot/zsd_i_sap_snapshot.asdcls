@EndUserText.label: 'Unrestricted'
@MappingRole: true
define role ZSD_I_SAP_SNAPSHOT {
    grant
        select
            on
                ZSD_I_SAP_SNAPSHOT where ( ) = aspect pfcg_auth ( I_TCODE, TCD = 'IQ09' );  
                
}