@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZSD_I_SHIPPING_POINT_TIME_ZONE {
    grant
        select
            on
                ZSD_I_SHIPPING_POINT_TIME_ZONE
                    where
                        inheriting conditions from entity i_deliverydocument;
                        
}