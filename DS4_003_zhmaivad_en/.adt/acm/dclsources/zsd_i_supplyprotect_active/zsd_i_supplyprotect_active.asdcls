@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZSD_I_SUPPLYPROTECT_ACTIVE {
    grant
        select
            on
                ZSD_I_SUPPLYPROTECT_ACTIVE
                    where
                        inheriting conditions from entity zsd_i_atp_relevantnetchange;
                        
}