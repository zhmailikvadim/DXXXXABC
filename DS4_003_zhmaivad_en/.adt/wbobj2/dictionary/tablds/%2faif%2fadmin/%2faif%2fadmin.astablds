@EndUserText.label : 'Additional information structure (be appent to tran. table)'
@AbapCatalog.enhancement.category : #EXTENSIBLE_ANY
define structure /aif/admin {
  add_1 : include /aif/additional_info_st1;
  add_2 : include /aif/additional_info_st2;
  include /aif/processing_timestamp;

}