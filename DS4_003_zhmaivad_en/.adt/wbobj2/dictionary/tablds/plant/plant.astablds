@EndUserText.label : 'Sequence table for MRP units - scope of planning'
@AbapCatalog.enhancement.category : #EXTENSIBLE_CHARACTER
define structure plant {
  plwrk : plwrk;
  berid : berid;
  berty : berty;
  puwnr : puwnr;
  pucnt : count;
  rncnt : count;

}