@EndUserText.label: 'Restricted'
@MappingRole: true

define role ZSD_C_SUPDMNDREJECTEDOBALLDOC {
    grant
        select
            on
                ZSD_C_SUPDMNDREJECTEDOBALLDOC
                    where
                        inheriting conditions from entity C_SupDmndAllDocTotDmndQry;
                        
}