@EndUserText.label: 'Restricted'
@MappingRole: true
define role ZHR_C_CANDIDATEHEADER {
    grant
        select
            on
                ZHR_C_CANDIDATEHEADER
                    where
                        created_user = aspect user;
                        
}